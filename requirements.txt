elasticsearch==8.18.1
google-genai==2.12.1
pandas==2.2.2
python-dotenv==0.21.0
# PyTorch 2.11+ defaults to CUDA-enabled wheels on Linux ARM. This application
# embeds on CPU, so keep Docker images small and deterministic.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1+cpu; platform_system == "Linux"
torch==2.7.1; platform_system == "Darwin"
torch==2.7.1+cpu; platform_system == "Windows"
sentence-transformers==4.1.0
streamlit==1.37.1
tqdm==4.66.5
